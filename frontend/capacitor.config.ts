import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.sentemon.fitnessapp',
  appName: 'FitnessApp',
  webDir: 'dist/frontend/browser'
};

export default config;
