apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.35.0 (HEAD)
  labels:
    io.kompose.service: workout-service
  name: workout-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: workout-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.35.0 (HEAD)
      labels:
        io.kompose.service: workout-service
    spec:
      containers:
        - env:
            - name: DatabaseConnectionString
              value: Host=postgres;Port=5432;Username=postgres;Password=mysecretpasswordfordevelopment;Database=WorkoutsDb
            - name: RabbitMq__Host
              value: rabbitmq
          image: workout-service
          livenessProbe:
            exec:
              command:
                - curl -f http://workout-service:8005/health || exit 1
            failureThreshold: 3
            periodSeconds: 10
            timeoutSeconds: 5
          name: workout-service
          ports:
            - containerPort: 8005
              protocol: TCP
      restartPolicy: Always
